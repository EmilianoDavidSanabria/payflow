[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "cd payflow && gunicorn payflow.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/core/health/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10